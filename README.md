# Skills
This repo contains skills useful for an academic workflow.
## Usage
1. Save the directory with its `SKILL.md` file to `~/.claude/skills/`.
2. Activate a skill in your session with e.g. `/read-paper`.
